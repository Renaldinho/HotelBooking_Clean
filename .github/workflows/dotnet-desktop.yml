name: .NET Core CI/CD

on:
  push:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    env:
      ASPNETCORE_URLS: 'https://+:5001;http://+:5000'

    steps:
      - uses: actions/checkout@v2

      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: '8.0.x' # Or your desired .NET version

      - name: Build
        run: dotnet build HotelBooking.WebApi/HotelBooking.WebApi.csproj --configuration Release

      - name: Start .NET application (background)
        run: dotnet Release/HotelBooking.WebApi.dll &

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Newman
        run: npm install -g newman

      - name: Run Newman tests
        run: newman run Postman/booking_tests -k -r cli